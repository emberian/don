# Canonical bounded GUARD runtime

This tranche mounts the dominant observed opcode-31 subdomain without promoting the GUARD
action, opcode, or general executor row. The admitted cone is one ordinary Unit selected by the
canonical fixed `Groups`/play-cache owner, a same-owner ordinary Unit target, and
`QueuePos::New`. A revision-bound content adapter supplies the singleton formation offset and the
target virtual answers that compact `World` does not own.

Pre-existing payload-free `OrderIndex::Guard` nodes remain a legacy/inert compatibility shape;
the production frame adapter refuses them. Canonical GUARD installation always creates tag 5,
and every tag-5 node is kind- and duplicated-target-validated.

The strict fixture is the exact packet from
`playback___2014.08.08_22_23_01__fri_.rcx`, package 1284, turn 1285, play 1, frame 75135:
Group `0001036e00`, GUARD `1f6a0000000300000002000000`. It selects Unit 110 and targets Unit
106 for owner 3. The corpus contains 108 GUARD packets: 106 use Queue-New and two use Queue-Last;
all effective selections and all targets are Unit-band.

The packet transaction publishes selection cache, fixed Group/backlink, cleared path, and a full
35-byte walked `GuardOrderState` together. DoNSave tag 5/version 1 retains the target identity,
`dx/dy`, snapped guard post, `idle`, and `retry`; kind/tag and duplicated-target mismatches reject.
The production frame adapter admits only the exact nonmoving periodic pulse
`(actor.o + frame) % 16 == 0`, where retail increments `GuardOrder::idle` without a spatial call,
RNG draw, animation, movement, or cast insertion. Save/load/resave and uninterrupted/resumed
frames are compared in the focused test.

Still red: multi-member formation/captain reshaping, Queue-Last and Queue-First insertion,
existing-order updates, scenario pruning, nearby-unit recruitment, spatial/trig/terrain search,
temporary Group MoveNear, same-tick movement, CAST insertion, and GUARD RNG. Consequently no
command-table or closure metadata changes are made.
