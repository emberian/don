# Copyrighted game content — never commit. Reproduce with the extractor documented in
# docs/binary-ground-truth.md (Provenance section).
ron-bin/
ron-data/

# Derived from copyrighted code, and large.
re/ghidra/
re/decomp/
re/logs/

# Build output
target/
*.rlib
__pycache__/
*.py[cod]

# Local agent/session handoff material. These files can contain private conversation context
# and machine-specific recovery data; they must never enter repository history.
.revive/
STATUS.md
RESUME.md
docs/RECOVERY.md
docs/DESIGN-INTENT.md

# Live captures are game data (copyrighted); reproduce with the probes in docs/.
schema/live/*.bin
schema/live/*.txt
# RoNtoy captures can contain player names, diplomacy/chat state, and other live-session
# material. Runtime output belongs here; only synthetic/redacted fixtures belong in git.
var/rontoy/
*.donfeed
schema/live/rontoy-*.json
schema/live/rontoy-*.ndjson
# Local playable-client smoke/performance reports contain timestamps and browser details.
web/play-results*.json
re/decomp-all/
ron-data/replays/
schema/bhs-compiler-regression.log

# Regenerable corpus index (~3.1 MB, ~100 s from gitignored ron-data/).
analysis/corpus-commands.json
