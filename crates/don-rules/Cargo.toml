[package]
name = "don-rules"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Parsers for Rise of Nations rule data, derived from the shipped files and binary."

[dependencies]
