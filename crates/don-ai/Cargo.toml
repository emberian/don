[package]
name = "don-ai"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Transcription of the shipped Rise of Nations production AI: the BHS host ABI, the AI cycle scheduler, and economic.bhs."

[dependencies]
